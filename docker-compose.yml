services:
  capi:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      ADMIN_PASSWORD: admin123
      SESSION_SECRET: change-me
      DB_PATH: /data/meta-capi.sqlite
    volumes:
      - meta-capi-data:/data
    restart: unless-stopped

volumes:
  meta-capi-data:
